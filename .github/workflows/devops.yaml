name: Demo CICD

on: 
  push:
    branches: [dev, master]

    pull_request:
      branches: [dev, master]

      
  
jobs:
  runs-on: ubuntu-latest # or docker image name
  job1:
    strategy:
      matrix:
        node-version: [ 16.x ]
    
    steps:
      - name: A bash command
        run: echo Hello from cicd file

      - name : install python
        run: apt-get install python3
 
